{
  "name": "ng-schedule",
  "version": "0.1.0",
  "description": "Schedule picker for Angular 1",
  "scripts": {
    "start": "concurrently \"gulp watch\" \"http-server ./ -c -1\"",
    "build": "gulp",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower install && typings install"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "~2.2.0",
    "http-server": "~0.9.0",
    "merge2": "~1.0.2",
    "gulp": "~3.9.1",
    "gulp-concat": "~2.6.0",
    "gulp-replace": "~0.5.4",
    "gulp-typescript": "~2.14.0",
    "gulp-sass": "~2.3.2"
  }
}